node_modules/
.next/
*.log

# Segredos (regra inviolável 1). Só .env.example é versionado.
.env
.env.local
.env.*.local

# Saída de execução do spike: trilha de auditoria e achados em JSON.
# Os documentos gerados (docs/*.md) e as fixtures (tests/fixtures/) SÃO versionados.
spike/out/
